{% if db_type != "none" %}
{% if asyncio_db %}
DATABASE_URL=postgresql+asyncpg://username:password@server:port/database
ALEMBIC_DATABASE_URL=postgresql+psycopg://username:password@server:port/database
{% else %}
DATABASE_URL=postgresql+psycopg://username:password@server:port/database
{% endif %}
{% endif %}
# uses sqlite database for testing if below is not set
# TEST_DATABASE_URL=postgresql+psycopg://username:password@server:port/test_database
# KEEP_TEST_DB=N

# univorn settings
WORKERS=1
